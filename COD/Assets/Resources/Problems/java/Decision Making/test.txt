case
switch
default
break
continue
elif
elif
||
//
and
##############################
using System;

class DecisionMaking
{
    public static void Main(String[] args)
    {
        bool hasEngine = true;
        bool hasWheels = true;
        bool hasHandlebars = false;

        (NODE_SLOT:if) (hasEngine (NODE_SLOT:&&) hasWheels && !hasHandlebars)
        {
            Console.WriteLine("It's a car!");
        }
        (NODE_SLOT:else if) (!hasEngine && hasWheels && hasHandlebars)
        {
            Console.WriteLine("It's a bicycle!");
        }
        (NODE_SLOT:else if) (hasEngine && hasWheels && hasHandlebars)
        {
            Console.WriteLine("It's a motorcycle!");
        }
        (NODE_SLOT:else)
        {
            Console.WriteLine("Unable to identify type of vehicle.");
        }
    }
}
##############################
It's a car!