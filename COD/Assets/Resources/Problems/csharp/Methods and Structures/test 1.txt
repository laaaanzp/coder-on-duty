float
number
string
double
int
##############################
using System;

public class Program
{
	public static (NODE_SLOT:int) Add(int a, int b)
	{
		int total = a + b;
		return total;
	}

	public static (NODE_SLOT:float) Add(float a, float b)
	{
		float total = a + b;
		return total;
	}

	public static (NODE_SLOT:double) Add(double a, double b)
	{
		double total = a + b;
		return total;
	}

	public static void Main(string[] args)
	{
	    int total = Add(3, 5);
		Console.WriteLine(8);
	}
}
##############################